import requestResponseLogEntry from "./requestResponseLogEntry";
export default class RequestResponseLog {
    static readonly defaultLogDirectory: string;
    static deleteInstance(): void;
    static getInstance(): RequestResponseLog;
    private static log;
    baseDirectory: string;
    private context;
    private entries;
    private constructor();
    addEntry(logEntry: requestResponseLogEntry): Promise<void>;
    getEntries(): Promise<requestResponseLogEntry[]>;
    setContext(context: string): Promise<void>;
    getFileName(): string;
    private xmlToJson;
    private assertDirectory;
}
